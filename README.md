# DevAICourse

Please check the petstore.yml and correct all endpoints implemented in the main.py to fit to the swagger.

I have a backend running locally, which is used in the configuration in my pets.tsx. But running the requests locally in my vite application to my backend is stopped with CORS error in Chrome. How to fix it?
